import { Meta, Story } from '@storybook/addon-docs/blocks'
import * as stories from '../player'

<Meta title='SpeechKit JS/Player' />

# Standard player

<Story name='player' height='60px'>{() => (stories.player())}</Story>

```js
SpeechKit.player({
  projectId: "6673",
  // One of the following parameters has to be provided
  podcastId: "534513",
  articleUrl: "...",
  externalId: "...",
}).then(appInst => {});
```

# Player with the ads

<Story name='playerWithAds' height='60px'>{() => (stories.playerWithAds())}</Story>
